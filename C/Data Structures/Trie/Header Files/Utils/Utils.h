#ifndef PROJETOFINALED_UTILS_H
#define PROJETOFINALED_UTILS_H

void notImplemented(char *function);

void wait(int mlSeconds);

void waitSeconds(int seconds);

char *toLower(char *s);

#endif //PROJETOFINALED_UTILS_H
